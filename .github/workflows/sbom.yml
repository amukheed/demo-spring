on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        run: mvn clean install --output buildOutput

      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v3.1.0
        with:
          path: buildOutput